import Vue from 'vue';
import router from '../router';

/* ==========================================================================
 *  这里定义一个可爱的 路由 小组件
 * ======================================================================== */

Vue.component('miniRouter', {
    //这里是这个参数是非常重要的，我之前没有用这个，困了我很久
    //上个git版本中，是在vue实例中使用了路由。
    //这里我尝试地在自定义组件中使用了 路由，其实和vue实例中的用法是一样！
    router,
    template: '<div class="fishLine"><router-view></router-view></div>',
});


